# Ransomware

```
usage: main.py [-h] [-e] [-d] directory

Ransomware

positional arguments:
  directory      directory to encrypt or decrypt

optional arguments:
  -h, --help     show this help message and exit
  -e, --encrypt  encrypt files
  -d, --decrypt  decrypt files
```