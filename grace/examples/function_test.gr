
def sum(x: int, y: int): int {
     return x + y;
}

def main(): int {
    var x, y, z, i: int;
    var b: bool;

    x = 2;
    y = 6;

    //b = y;

    while (z > 10) {
        z = z - x;

        for (i = 0; i < 10; i = i + 1) {
            z = sum(sum(x, y), 100 * i);
        }
    }

    return 0;
}
